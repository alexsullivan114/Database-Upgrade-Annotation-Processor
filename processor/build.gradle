apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compileOnly 'com.google.auto.service:auto-service:1.0-rc2'
    compileOnly project(':SQLiteUpgradeHelper')
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
